# leash — environment variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# Default scanning mode (patrol|sweep|lockdown|off)
LEASH_DEFAULT_MODE=patrol

# npm token (for publishing releases)
# NPM_TOKEN=your-npm-token-here
